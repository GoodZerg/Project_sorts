#include "Sorts.h"

int main() {
  std::vector<int> sizes;

  sizes.push_back(10);
  /*
  sizes.push_back(150);
  sizes.push_back(300);
  sizes.push_back(600);
  sizes.push_back(1000);
  sizes.push_back(10000);
  */
  for (auto i : sizes) {
    interface n(i);
  }

  system("pause");
  return 0;
}